---
date: 2026-06-07
repo: DecompilingFutureCrew
status: open
resume: "verder met DecompilingFutureCrew — fase 2 v0.2.0-Trug: eerste echte decompile GLENZ (translucent vector objects) op basis van source/GLENZ asm; mesh + additive blending + scene-integratie in timeline"
---

# 001 — v0.1.0-Marvel (fase 1 — renderer-fundamenten)

**Datum:** 2026-06-07
**Trigger:** "verder" → mini-WhatIf → "akkoord"

## Doel

Fase 1 uit `BUILD_PLAN.md` afronden: WebGL2-wrapper compleet maken zodat scenes shaders kunnen registreren, HUD toevoegen, input uitbreiden met long-press/double-tap, en één echte scene (STARFIELD) als bewijs dat de wrapper werkt.

## Wat gemaakt

| Bestand | Wijziging |
|--|--|
| `src/engine/renderer.ts` | uitgebreid: `program(key, vs, fs)` met cache, `drawFullscreen(prog, setUniforms)`, cached fullscreen-triangle VAO, `Framebuffer` class |
| `src/engine/timeline.ts` | `paused` state, `back()`, `togglePause()`, `currentName` getter |
| `src/engine/input.ts` | long-press 500ms = pause, double-tap 280ms = back, kbd: Space/Esc = pause, →/Enter = skip, ← = back, H = HUD toggle |
| `src/engine/hud.ts` | **nieuw** — DOM-overlay met FPS, scene-naam, paused-indicator |
| `src/scenes/starfield.ts` | **nieuw** — procedural 3-laags parallax starfield via fragment-shader (twinkle + vignet) |
| `src/scenes/start.ts` | **verwijderd** (vervangen door STARFIELD) |
| `src/main.ts` | registreert HUD + alle input-callbacks |
| `package.json` | bump `0.0.1` → `0.1.0` |
| `package-lock.json` | **nieuw** (11 deps) |
| `CHANGELOG.md` | sectie `[0.1.0-Marvel]` toegevoegd |
| `BUILD_PLAN.md` | Fase 1 afgevinkt, fase 2 wordt huidige |

## Verificatie

- `npx tsc --noEmit`: schoon
- `npm run build`: 11 modules, **8.34 KB JS / 3.49 KB gzipped** (cold-start-budget 150 KB ruim gehaald)
- `npm run dev` op `http://localhost:5173/DecompilingFutureCrew/`: HTML + alle module-endpoints (main/renderer/starfield/hud/input) leveren 200
- **Pending — gebruiker:** GLSL-shader-correctheid in echte browser; verwacht: zwart canvas met sterren in drie parallax-lagen die langzaam horizontaal scrollen, twinkle-effect, lichte vignet — daarna 4s rode ALKU-fade, dan loop.

## Open / parkeer

- Fullscreen-API niet geïmplementeerd (uitgesteld naar 0.1.1 als gewenst)
- HC55 mirror nog niet opgezet (gepland later)
- Audio-tracker player nog stub (fase 4)

## Volgende — fase 2 GLENZ

Eerste echte decompile-target. Stappen:
1. Lees `SecondReality_source/GLENZ/` — identificeer mesh-data en transform-routine
2. `decomp/glenz/ASM_NOTES.md` invullen met asm-citaten (file:line) en uitleg
3. `decomp/glenz/port.ts` — mesh + matrix-pipeline in TypeScript
4. `port.vert` / `port.frag` — additive-blending vector-render
5. Registreer als `src/scenes/glenz.ts` in timeline tussen STARFIELD en ALKU
6. Bump naar `v0.2.0-Trug` (Mikko Tukiainen)
