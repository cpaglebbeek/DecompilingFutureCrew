---
date: 2026-06-10
repo: DecompilingFutureCrew
status: open
resume: "verder met DecompilingFutureCrew GLENZ (v0.8.3-Wildfire). Open: (1) audio-verdict via /glenz/music.html — luister A (Opus-loop) vs A2 (lossless WAV) vs REF; is A hakkerig op de loop-naad (→ loop-trim) of lopen A+A2 trager dan REF (→ tempo-warmup render)? (2) physics-fixes #2 depth-collapse-zichtbaarheid / #3 eeuwige-loop-exit / #4 tick-smoothing — akkoord-afhankelijk, na visuele bevestiging dat fix #1 framing klopt. (3) Optioneel: deploy naar GitHub Pages + HC55-mirror (deploy/deploy-hc55.sh). (4) Klein: hoofddemo-HUD toont nog hardcoded v0.6.0-Yodel i.p.v. v0.8.3."
---

# 007 — GLENZ (Wildfire) — fysica-framing, viewer, muziek-debug, navigatie

**Trigger:** deep-dive beeld/physics → "bij het origineel deukt de polygoon in, in jouw versie explodeert hij" → daarna "fix viewer en music debug page" → "ik zie nog geen optie voor viewer versie en music.html opent een ander deel van de demo".

Deze sessie sloot de GLENZ-werkstroom af die sinds v0.5.1 ongecommit op schijf stond (hele `glenz/`-tree + audio-assets waren untracked). Eerste git-vastlegging van het GLENZ-deel.

## Wat is gedaan (chronologisch)

### v0.8.1-Wildfire — physics-framing (geel)
- Diagnose: de jello-squash (`MAIN.C:526-527`) is een faithful port in `glenz_core.ts:227-228`; ook de transform-volgorde (rotate-then-scale in scherm-assen) klopt. De waargenomen "explosie" zat **niet** in de physics maar in de **framing**: object vulde het frame, waardoor de ±27% squash schermvullend werd.
- Fix: `Z_POS 7100 → 9500` → object compact met zwarte marge, matcht `ref_10/11`. Squash-amplitude ongewijzigd, leest nu als deuk.

### v0.8.2-Wildfire — viewer + muziek-pagina permanent (geel)
- `buildViewerB()` deelt nu A's oriëntatie (`rotX=vRx, rotY=vRy`) i.p.v. demo-counter-rotatie → translucent samenstel draait als één star object in viewer-mode.
- `music.html` + debug-assets verplaatst naar `public/` zodat `vite build` (`emptyOutDir`) ze niet meer wist.

### v0.8.3-Wildfire — navigatielaag (geel)
- Diagnose: drie geïsoleerde pagina's (hoofddemo `/`, GLENZ-standalone `/glenz/`, muziek-debug `/glenz/music.html`) zonder onderlinge links; elke pagina werkt alleen op het base-prefixte pad (`/DecompilingFutureCrew/...`). `music.html` zonder base → 404 → host valt terug op hoofd-index = "ander deel van de demo".
- Fix (3 base-veilige relatieve links):
  - `index.html`: hoek-link rechtsboven `GLENZ · viewer & muziek →` → `glenz/`.
  - `glenz/index.html`: derde knop `🎵 Muziek-debug` naast Demo/Viewer → `./music.html`.
  - `public/glenz/music.html`: terug-links `← GLENZ (demo/viewer)` + `← hoofddemo`.
- Geverifieerd: build groen, preview 200 op alle drie de paden, 3 browser-screenshots.

## Open punten (zie resume)
1. Audio-verdict via music.html (loop-trim vs tempo-warmup).
2. Physics-fixes #2/#3/#4 (akkoord-afhankelijk).
3. Deploy (Pages + HC55).
4. HUD-versiestring bijwerken (v0.6.0-Yodel → v0.8.3).

## Repo-hygiëne
- `.gitignore`: probe-entries geconsolideerd tot patroon `_glenz_*.mjs` (40 wegwerp-probes blijven op schijf, uit commit).
